# Production Environment Variables
# Copy this file to .env and update with your production values
# Usage: cp .env.example .env

# ==============================================================================
# POLLER SERVICE CONFIGURATION
# ==============================================================================

# Traccar API Configuration
TRACCAR_BASE=http://93.127.139.107:8082/api
# Alternative development endpoint:
# TRACCAR_BASE=http://69.197.176.231:8082/api

# Tunnel/Sender URL (where poller forwards data)
# For Docker: use service name 'sender'
# For local development: use localhost
TUNNEL_URL=http://sender:4000/traccar/
# Alternative for local development:
# TUNNEL_URL=http://localhost:4000/traccar/

# Node Environment
NODE_ENV=production

# ==============================================================================
# SENDER SERVICE CONFIGURATION
# ==============================================================================

# Sender Service Port
PORT=4000

# BSMART/Latra API Configuration
BSMART_API_URL=http://vts.latra.go.tz:8090/data-integration/integration/gps
BSMART_API_TOKEN=d2ViY29ycGx0ZDp3ZWJjMHJwQDIwMjU=

# ==============================================================================
# OPTIONAL CONFIGURATION
# ==============================================================================

# Traccar Authentication (if needed in future)
# TRACCAR_USERNAME=admin
# TRACCAR_PASSWORD=admin

# Logging Level (if implementing custom logging)
# LOG_LEVEL=info

# Polling Interval (milliseconds) - default: 1000
# POLLING_INTERVAL=1000

# Request Timeout (milliseconds)
# REQUEST_TIMEOUT=10000

# ==============================================================================
# DOCKER-SPECIFIC NOTES
# ==============================================================================
# When running in Docker Compose:
# - Use service names (e.g., 'sender') instead of 'localhost'
# - The services communicate via the traccar-network bridge network
# - Ports are mapped to host: 2000 (poller), 4000 (sender)
#
# When running locally (without Docker):
# - Use 'localhost' or '127.0.0.1'
# - Ensure both services are running on their respective ports
# ==============================================================================
