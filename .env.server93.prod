# Production Environment Variables for Server 93 (93.127.139.107)
# This server runs ONLY the Poller service
# Copy this file to .env before deployment: cp .env.server93.prod .env

NODE_ENV=production

# Traccar Configuration (local on server 93)
# Use localhost or 127.0.0.1 since Traccar is on the same server
TRACCAR_BASE=http://localhost:8082/api
TRACCAR_USERNAME=admin
TRACCAR_PASSWORD=admin

# Sender URL (remote on server 69)
# This is where the poller will send data
TUNNEL_URL=http://69.197.176.231:4000/traccar/

# Optional: Uncomment to override default port
# POLLER_PORT=2000
